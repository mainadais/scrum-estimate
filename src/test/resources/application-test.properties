#database connection properties
spring.datasource.platform=org.hibernate.dialect.PostgresDialect
spring.datasource.url=jdbc:postgresql://localhost:5432/db_scrum_estimate
spring.datasource.username=postgres
spring.datasource.password=admins
#initialize database using schema.sql for testing purposes
spring.datasource.initialization-mode=always
spring.datasource.schema=classpath:/schema.sql
spring.datasource.data=classpath:/data.sql
#override bean definitions for testing purposes only
spring.main.allow-bean-definition-overriding=true
#jwt security
jwt.passwordStrength=10
jwt.tokenIssuer=${spring.application.name}
jwt.tokenSecret=and0LnNlY3JldA==